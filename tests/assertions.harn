/** Require that a smoke-test condition is true. */
pub fn expect(condition: any, message: any = "expectation failed") {
  require condition, message
}

/** Require that two smoke-test values are equal. */
pub fn expect_eq(actual: any, expected: any, message: any = "values differ") {
  require actual == expected,
    message + ": expected " + to_string(expected) + ", got "
    + to_string(actual)
}
